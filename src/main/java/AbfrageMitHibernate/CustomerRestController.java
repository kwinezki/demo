package AbfrageMitHibernate;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.List;

//@RestController
public class CustomerRestController {
/*
    private CustomerService customerService;                                //interne Variable

    public CustomerRestController(CustomerService customerService) {        //Konstruktor
        this.customerService = customerService;
    }

    @GetMapping("/list-customers-bauer")
    public CustomerDto getCustomerBauer() {
        List<Customer> customerList = customerService.loadCustomerBauer(); //Abfrage auf der Datenbank nach Nachname Bauer wird durchgeführt

        CustomerDto customerDto = new CustomerDto();                          //Ein Objekt CustomerDto mit dem Inhalt der customerList wird erstellt
        customerDto.setCustomer(customerList);

        return customerDto;                                                 //Das ganze CustomerDto Objekt wird als JSON zurück gegeben
    }
*/
}
